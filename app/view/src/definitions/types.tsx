export type DialogStatus = true | false;

export type LoginInputs = {
    username: string
    password: string
};

export type SignupInputs = {
    username: string
    password: string
    password2: string
};